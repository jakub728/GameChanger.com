{
  "name": "GameChanger",
  "version": "1.0.0",
  "description": "This is a GameChanger internet site",
  "author": "JS",
  "scripts": {
    "start": "rm -rf dev-serve && parcel src/index.html --dist-dir dev-serve",
    "build": "rm -rf build && parcel build src/index.html --dist-dir docs --public-url ./"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.0"
  }
}
